/**
 * AI 호출 클라이언트 제한 시간(ms)
 *
 * Cloud Functions의 `timeoutSeconds: 120`보다 위에 둔 안전망. 서버 예산보다 짧으면
 * 서버가 확보한 시간을 쓰기 전에 브라우저가 요청을 끊어, 호출 비용만 나가고 결과는
 * 받지 못함. 같은 값으로 맞추면 서버가 응답을 보내는 시점과 경쟁해 실제 서버 오류
 * 대신 시간 초과가 표시됨.
 *
 * 정상 경로의 제한은 서버가 정하고, 이 값은 응답이 클라이언트에 도달하지 못하는
 * 경우(네트워크 전환, 절전 복귀, 중간 프록시)에만 걸림.
 */
export const AI_REQUEST_TIMEOUT_MS = 130_000;
